<Window x:Class="CopilotSessionTray.App.SessionSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Session summary"
        Width="360" Height="320"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="11" Foreground="Gray" TextWrapping="Wrap"
                   Text="Renaming only changes this app's own local label — it never touches the real Copilot CLI session." />

        <StackPanel Grid.Row="1" Margin="0,10,0,4">
            <TextBlock FontSize="11" FontWeight="SemiBold" Text="Session name" />
            <TextBox Margin="0,2,0,0"
                     Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>

        <TextBlock Grid.Row="2"
                   Margin="0,10,0,4"
                   FontSize="11" FontWeight="SemiBold"
                   Text="Summary (from Copilot CLI's own checkpoints)" />

        <Border Grid.Row="3"
                BorderBrush="#DDDDDD" BorderThickness="1"
                Background="#FAFAFA">
            <TextBox Text="{Binding Summary, Mode=OneWay}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     VerticalScrollBarVisibility="Auto"
                     BorderThickness="0"
                     Background="Transparent"
                     Padding="6" />
        </Border>

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Padding="10,4" Content="Close" Click="Close_Click" IsDefault="True" IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
